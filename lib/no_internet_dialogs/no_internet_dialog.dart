import 'package:dialog/dialog1.dart';
import 'package:dialog/no_internet_dialogs/dialogs/no_internet_layout1.dart';
import 'package:dialog/no_internet_dialogs/dialogs/no_internet_layout2.dart';
import 'package:dialog/no_internet_dialogs/dialogs/no_internet_layout3.dart';
import 'package:flutter/material.dart';

class NoInternetDialog {
  static showNoInternetDialog(BuildContext context) {
    // showDialog(
    //   context: context,
    //   builder: (context) => NoInternetLayout1(isDark: false),
    // );
    // showDialog(
    //   context: context,
    //   builder: (context) => NoInternetLayout2(isDark: false),
    // );
    // showDialog(
    //   context: context,
    //   builder: (context) => NoInternetLayout3(isDark: true),
    // );
  }
}
